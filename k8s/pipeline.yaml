apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: mlops-pipeline-
  namespace: argo
spec:
  entrypoint: ml-workflow
  
  # PVC для обмена файлами (самый простой способ)
  volumeClaimTemplates:
  - metadata:
      name: workdir
    spec:
      accessModes: [ "ReadWriteMany" ]
      resources:
        requests:
          storage: 1Gi

  templates:
  - name: ml-workflow
    dag:
      tasks:
      - name: load-data
        template: run-step
        arguments:
          parameters:
          - name: cmd
            value: "python3 src/load_data.py --output /mnt/data"

      - name: split-data
        dependencies: [load-data]
        template: run-step
        arguments:
          parameters:
          - name: cmd
            value: "python3 src/split_data.py --data-dir /mnt/data"

      - name: train-model
        dependencies: [split-data]
        template: run-step
        arguments:
          parameters:
          - name: cmd
            value: "python src/train.py --data-dir /mnt/data --params {{inputs.parameters.params}}"
          - name: params
            value: "n_estimators=150,max_depth=10,min_samples_split=2,min_samples_leaf=1"

      - name: validate-model
        dependencies: [train-model]
        template: run-step
        arguments:
          parameters:
          - name: cmd
            value: "python3 src/validate.py --data-dir /mnt/data"

  # Универсальный шаблон запуска
  - name: run-step
    inputs:
      parameters:
      - name: cmd
    container:
      image: nickosipov/otus-argo-workflows:latest
      imagePullPolicy: IfNotPresent
      command: ["/bin/sh", "-c"]
      args: ["{{inputs.parameters.cmd}}"]
      volumeMounts:
      - name: workdir
        mountPath: /mnt/data